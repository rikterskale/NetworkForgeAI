# Compatibility requirements file.
#
# pyproject.toml is authoritative. This file delegates to the same package
# metadata used by wheels and Docker images so source installs do not drift
# from the declared dependency graph.
-e .[runtime,llm]
